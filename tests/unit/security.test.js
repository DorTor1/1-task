import { describe, it, expect } from 'vitest';
import helmet from 'helmet';
describe('helmet defaults', () => {
    it('helmet is a function', () => {
        expect(typeof helmet).toBe('function');
    });
});
//# sourceMappingURL=security.test.js.map